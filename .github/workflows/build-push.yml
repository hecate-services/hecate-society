# Build the OCI image for hecate-society and push to ghcr.io.
#
# GitHub is canonical since 2026-07-26, so this runs where the code lives.
# Pushes BOTH :latest and the semver tag. The deployed compose tracks :latest
# under watchtower for zero-touch deploys, and the semver tags stay on ghcr so a
# bad release can be pinned back to a known one.
#
# NO EXPERIMENT TAG YET, AND DELIBERATELY. The predecessor tagged every image
# `world-N' from the number its own physics reported, which is a good mechanism
# and worth copying back the day there is a numbered thing to copy it for. A tag
# derived from a number that does not exist yet is a tag that lies.

name: build-and-push

on:
  push:
    branches: [main]
    tags:     ['v*']
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Log in to ghcr.io
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set tag
        id: tag
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/v* ]]; then
            echo "tag=${GITHUB_REF#refs/tags/v}" >> "$GITHUB_OUTPUT"
          else
            echo "tag=latest" >> "$GITHUB_OUTPUT"
          fi

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Containerfile
          push: true
          tags: |
            ghcr.io/hecate-services/hecate-society:latest
            ghcr.io/hecate-services/hecate-society:${{ steps.tag.outputs.tag }}
